import {getUserTempId} from '@/utils/userAbout';

const state = {
  userTempId: getUserTempId(),
};
const mutations = {};
const actions = {};
const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
